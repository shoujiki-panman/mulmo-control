# 進捗ボード — mulmo-control（2026-08-25）

## ✅ 完了したこと

- [x] v1.0.0 リリース公開（ユニバーサルバイナリ、Releases からのダウンロード方式）
- [x] install.sh の前提チェック（macOS 14 / npm / MulmoClaude / Swift コンパイラ、全て失敗経路を実測済み）
- [x] 2台の実機でインストール成功（M5 Air / 旧 Air。旧機は npm 回線エラー1回→再実行で完走）
- [x] BootCamp Slack で共有、最初の外部ユーザーの導入つまずき報告→修正済み
- [x] プロジェクト化（4点セット + Issue 一覧）
- [x] 別スレで PR #1〜#3 マージ済み（メニューバー案内・更新報告の正直化・MulmoClaude 任意化）
- [x] 2026-08-10: 自動確認で Claude ログイン / RemoteHost 状態も更新するようにした
- [x] 2026-08-10: MulmoClaude パスに空白や `'` が入ってもログイン復旧コマンドと app-info.env が壊れないようにした

## 🔨 いまやっていること

- **v1.0.53 まで公開済み。** v1.0.48 から Developer ID で署名し、公証を通してチケットを貼っている（#54）。**ブラウザで落とした zip がそのまま開く。** 1年越しのゴールだった #15 の前提条件だった
- リリースは `./release.sh X.Y.Z` の1コマンド（#14）。配る前後の確認も同じ script の中に入っている。**出したあと、利用者と同じ URL から zip を取り直して版・同梱スクリプト数・署名を検証するところまで含む**
- 検査カタログは **108項目 / うち ✅66 が `check.sh` で自動**（`SECURITY.md`）。**実装があることと、壊れたら気づけることは別**という原則で運用している
- 2026-08-25: 利用者の報告と実地の不具合から3件。**#139**（停止中に起動ボタンが出ない）**#141**（起こす MulmoClaude が PORT を引き継いで落ちる）**#143**（ダークモードで文字が読めない）。3件とも実機で目視・実測してからマージした
- open は3本。**#6**（作者以外の環境で通っていない）**#23**（スマホ連携の再接続）**#67**（検査カタログ）。いずれも本人の手か別のマシン、あるいは「本当に切れる瞬間」が要る

## ⏭ 残り

- GitHub の Issues 一覧が正。ここには書き写さない

## ⚠️ ハマりどころメモ

- アプリの入手経路が2つある（Releases ダウンロード / 手元ビルド）。テストは両方通すこと（MULMO_CONTROL_ZIP_URL で失敗を再現できる）
- クリーンルーム検証: 空の HOME を作って `env HOME=... ./install.sh`（/Applications への書き込みだけ sed で退避先に差し替える）
- 旧 Air の npm は ECONNRESET が出やすい（回線起因、再実行で通る）
- **リリースは手で組まない。`./release.sh` を使う。** v1.0.9 は配信物を確かめずに出して、ほとんどのボタンが動かない版を配った。script には配る前後の確認が入っている
- **アプリのパスには空白がある**（`/Applications/Mulmo Control.app`）。コマンド文字列にパスを埋めるときは必ず `tool()` / `bin()` を通す。裸で埋めると `/Applications/Mulmo` で切れる
- **`.buttonStyle(.plain)` の当たり判定は描画された中身に従う。** 背景が透明なボタンは文字の上でしか押せないので `contentShape` を付ける
- **同梱スクリプトは署名に封入される**（`Sealed Resources ... files=23`）。`scripts/` の中身を1バイトでも書き換えると `codesign --verify` が落ちるので、配布物のスクリプトを手で直さない。直すならソースを直して `build-app.sh` からやり直す
- **積んだ PR を `--delete-branch` でマージしない。** 土台のブランチが消えた瞬間、そこを向いている PR は GitHub に CLOSED にされる。閉じた PR は base を変更できないので、その場では直せない。2026-08-25 に #140 → #142 → #144 を積んだとき実際に踏んだ。下から順にマージし、1本ごとに上の PR の base を `gh pr edit --base main` で向け直し、squash で変わった SHA を `git rebase --onto` で落とす。ブランチの削除は全部終わってから
- **作者の Mac がライトモードなら、暗いときの画面は一度も出ない。** #143 はまさにそれで、`Palette` の8色が明るいとき用の決め打ちだった一方、台紙だけ `.ultraThinMaterial` で外観についていくため、夜は文字だけが取り残されていた。補助文字のコントラスト比は **1.00:1**（背景と完全に同じ明るさ）。**目で気づくことに頼れない種類の不具合なので、`check.sh` 側に落とす**（108番）。#6 と同じ穴
- **このアプリが持っている環境変数は、起こす相手に全部伝わる。** MulmoTerminal のセルから Mulmo Control を起動すると `PORT=34567`（MulmoTerminal のポート）を持つ。#141 はそれが `yarn dev` まで素通りして、MulmoClaude のバックエンドが他人のポートを掴みにいっていた。厄介なのは **vite だけ数秒立つので生存判定が「動作中」と言う**こと。中身が空なだけで、直ったように見える
- **自己更新はバンドルの外に逃げてから走る**。`install.sh` が `/Applications/Mulmo Control.app` を `rm -rf` する一方、`mulmo-control-self-update` はその中から実行されるため。zsh はスクリプトを少しずつ読むので、消えたファイルの続きを読むと途中で死ぬ（`MULMO_SELF_UPDATE_DETACHED` で一度だけ再 exec している）
