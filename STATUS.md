# 進捗ボード — mulmo-control（2026-08-28）

## ✅ 完了したこと

- [x] v1.0.0 リリース公開（ユニバーサルバイナリ、Releases からのダウンロード方式）
- [x] install.sh の前提チェック（macOS 14 / npm / MulmoClaude / Swift コンパイラ、全て失敗経路を実測済み）
- [x] 2台の実機でインストール成功（M5 Air / 旧 Air。旧機は npm 回線エラー1回→再実行で完走）
- [x] BootCamp Slack で共有、最初の外部ユーザーの導入つまずき報告→修正済み
- [x] プロジェクト化（4点セット + Issue 一覧）
- [x] 別スレで PR #1〜#3 マージ済み（メニューバー案内・更新報告の正直化・MulmoClaude 任意化）
- [x] 2026-08-10: 自動確認で Claude ログイン / RemoteHost 状態も更新するようにした
- [x] 2026-08-10: MulmoClaude パスに空白や `'` が入ってもログイン復旧コマンドと app-info.env が壊れないようにした

## 🔨 いまやっていること

- **v1.0.53 まで公開済み。** v1.0.48 から Developer ID で署名し、公証を通してチケットを貼っている（#54）。**ブラウザで落とした zip がそのまま開く。** 1年越しのゴールだった #15 の前提条件だった
- リリースは `./release.sh X.Y.Z` の1コマンド（#14）。配る前後の確認も同じ script の中に入っている。**出したあと、利用者と同じ URL から zip を取り直して版・同梱スクリプト数・署名を検証するところまで含む**
- 検査カタログは **178項目 / うち ✅135 が `check.sh` で自動**（`SECURITY.md`）。**実装があることと、壊れたら気づけることは別**という原則で運用している
- 2026-08-25: 利用者の報告と実地の不具合から3件。**#139**（停止中に起動ボタンが出ない）**#141**（起こす MulmoClaude が PORT を引き継いで落ちる）**#143**（ダークモードで文字が読めない）。3件とも実機で目視・実測してからマージした
- 2026-08-28: **#145**。「スマホ連携（MulmoClaude）」が橙のまま戻らない理由を追った。ターミナル側だけ自動で繋がるように見えていたが、**仕組みは両者まったく同じ**で、差は UI が常時ブラウザで開いているかどうかだけだった。session blob はホスト側ではメモリにしか無く、永続化しているのはブラウザの localStorage だけ。**Mulmo Control が Keychain に鍵を預かる**ようにして、MulmoClaude が止まっていても1押しで繋げるようにした
- 2026-08-28: **#145 を通しで実測した。** MulmoClaude を止め、ブラウザのタブも閉じた状態から、起動 → 3001 の応答待ち → 預かった blob で reconnect → 緑まで **14.6秒**。#23 に「成功経路は未確認」と残っていた穴も埋まった
- 2026-08-28: **#147**。#23 に「ターミナル側の成功経路は未確認」と残していたので確かめたら、**成功しなかった**。繋がっている（`connected=True / health=online`）状態で繋ぎ直すと 401 が返り、「有効期限が切れています」と嘘をつく。#145 で MulmoClaude 側に入れた引き返しが、ターミナル側には無かった。**ガード112 も片側にしか掛かっておらず、検査がその非対称を見つけられなかった**ので、109 と合わせて両方に掛け直した
- 2026-08-28: **#149**。直近5件のうち4件（#139 #143 #145 #147）が「**アプリが世界の状態について嘘をつく**」同じ形だったので、そこを検査に落とした。`check.sh` は main.swift を**文字列として grep するだけで一度も動かしていなかった**。表示を決める部分を `Sources/StatusDisplay.swift` に分け、状態の全12通りを `swiftc` で組んで**実際に走らせる**ようにした。ガード112 が行番号を見ているのは動かせなかったからで、その制約が1つ外れた
- 2026-08-28: **#67 に着手。** 未着手39件のうち fast は13件で、そのうち版の比較（041 / 043 / 044 / 045）を落とした。比較だけ `scripts/mulmo-version-status` に切り出し、10通りを**実際に動かして**突き合わせる。**1.0.9 と 1.0.53 は文字列比較だと逆になる** — 新しい版を入れた人に更新を勧め続ける形になるので、そこを表に入れた
- 2026-08-28: **#152 に着手**（プロジェクト単位のリモートセッション）。CLI を実際に叩いて仕様を3か所直した。**ポートは存在しない**（`--remote-control` は名前で辿り着く）、**状態は自前で持たなくていい**（`claude agents --json` が cwd 付きで返す）、**起動には TTY と初回の人間の同意が要る**。①一覧表示と②ワンポチ接続まで。**PTY を切り離せば端末なしでセッションが生き残る**ことを実測し、`/rc active` と claude.ai/code の URL が出るところまで確認した。信頼フラグをこちらから書かないことをガードにした
- 2026-09-11: **#187**。利用者の報告から。`起動` を3回・`更新` を4回、33秒の間に押したら `yarn server` が2本（3001 と 3002）孤児として残り、翌朝の定期通知が4重に届いた。**ポートを見るガードは、ポートが立つ前の1分を見られない** — 通常モードの起動は画面を作り直すのに約1分かかり、その間は何も listen していないので連打が全部通る。しかも MulmoClaude はポートが埋まると 3002, 3003… へ逃げるので、2本目以降はそのガードの視界に入らない。直したのは3層で、**プロセスでも見分ける**（綴りを列挙し、さらに本体が node / yarn / npm であることまで見る。同じフォルダの node を全部拾うと人の claude セッションを落とす）・**錠で起動と更新を1本ずつにする**（ハードリンクで取るので、錠が見えた時点で持ち主が必ず分かる）・**画面側も処理中はボタンを押させない**（押せるのに効かないボタンが出ていたことが入口だった）。ついでに Telegram ブリッジを起動・停止で面倒を見るようにした（既定は off）
- 2026-09-11: **#190**（#187 の続き）。#187 では逃げた先を「見つけて止める」までやったが、**アドレスは 3001 決め打ちのままだった**ので、3002 で立っているものに `開く` が届かない。プロセスから実際の listen ポートを読む形にした。ついでに `.env` で `PORT` を変えている人にも合うようになった（#187 以前からズレていた）。**画面は `lsof` を叩けない**（1回 210ms、メニューを開くたびには払えない・#38）ので、スクリプトが控えを書き、画面はファイルを1つ読む
- 2026-09-11: **#192**。#187 で Telegram の行を足したらパネルが画面からはみ出し、**一番下の `終了` が押せなくなった。** 足すたびに余白を縮める直し方では次に足した人がまた踏むので、最初はスクロールさせようとしたが、メニューのパネルでそれは違うと言われて取り下げた。**効いていたのは増えた行より、増えたカードの器のほう** — カードは1枚ごとに余白 28pt と間隔 15pt を食うので、トグル2つと追加ツールで 120pt を超えていた。器を1枚にまとめて中を細い線で区切る形（CodexBar のメニュー部分と同じ）にした。ついでに Telegram の行の説明を書き直した（オフのときは「オンにすると何が変わるか」を書く）。**記号のガードを入れたら、別の場所に既存で1件すり抜けていた**
- 2026-09-11: **#198**。リリースは `./release.sh` の1コマンドだったが、**手元の Mac で 5〜10 分見張る**必要があった（公証の待ち）。同じ日に3回出して、そのたびに端末の前で待った。Actions の画面でバージョンを入れてボタン1回で出す形にした。手順は `release.sh` にそのまま任せ、ワークフローは資格情報を一時キーチェーンに入れて呼ぶだけ。**入口は手動だけ**にした。タグ push でも出るようにすると入口が2つになり、#187 の連打と同じ形で二重に出る。預ける作業も `./release-setup.sh` の1コマンド。**ガードを書いて壊してみたら、名前に数字が入る Secret（`P12`）を正規表現が取りこぼしていた**。**v1.0.69 を実際に GitHub 側から出した。** 押してから公開まで 1分45秒。手元で待っていた 5〜10分が、押して離れる時間になった
- 2026-09-16: **#183**。「前回の更新」の詳細が長い日に、パネルが画面の下からはみ出していた（#192 と同じ壊れ方で、一番下の `終了` に手が届かなくなる）。**記録は減らさない** — 何が動いて何が動かなかったかは、あとから理由を辿るための記録（#104）。出す場所を移して、追加ツールの行と同じ `>` で押したときだけ開く形にした。中は上限つきのスクロールなので、記録が何行に伸びても外側の高さは変わらない。行に出すあらまし（`9/15 22:06 ・ 4件を更新`）は `StatusDisplay.swift` に切り出して6通りを**実際に走らせている**。数えられない形の記録が来たときは、作った要約ではなく書いてある行をそのまま出す
- 2026-09-17: **#202**。ホバーガイドが出ないという報告から。アプリ側は無実で（`curl 127.0.0.1:34599/mt-guide` は `{"on":true}`、設定も ON）、**Tampermonkey がどのブラウザにも無かった**（**訂正 2026-09-20: 「消えた」のではなく最初から入っていなかった。本人は拡張を入れない方針。#207**）。ユーザースクリプトの本体も**ディスク上のどこにも無かった** — #185 でアプリ側（サーバーとトグル）だけを取り込み、肝心のスクリプトはリポジトリの外＝拡張の中にしか置いていなかった。**実装が1箇所しか無く、それが追跡していない場所だった**のが本当の原因。作り直して `userscript/` に置き、ガード164（本体の存在・接続先ポートが Swift と一致・MulmoTerminal のポートを決め打ちしない）を足した。当てる先は見えている文字ではなく `data-testid` / `aria-label` にしてある（上流が言い回しを変えても、間違った説明ではなく無言になる）。実画面に流し込んで、印つきの要素・`aria-label` だけの要素・説明の無い場所の3通りを実測
- 2026-09-20: **#205**。更新した直後、パネル上部の知らせ（「一括更新しました」）が更新内容を全部並べていた。#183 で環境タブの「前回の更新」は畳んだが、**同じ記録を上部がもう一度全部広げていた**。同じあらまし（`lastUpdateDigest`）を通して1行＋`>` にし、詳細は押したときだけ開く。1行で収まる知らせは畳まない。ついでに、**1件だけ動いた日は「1件を更新」ではなく `MulmoClaude: 1.18.0 → 1.19.0` をそのまま出す**ようにした（数より行のほうが言えることが多い）。ガード165
- 2026-09-20: **#207**。#202 の直しは Tampermonkey 前提だったが、**本人は拡張を入れない方針で、Tampermonkey は最初から入っていなかった**。#202 で「拡張ごと消えた」と書いたのは、ページに残っていた印（`mt-hover-guide-on`）を「前は動いていた」と読んだ誤り — 実際は前のセッションがブラウザ操作でスクリプトを流し込んで試した跡とみられる。拡張なしで MulmoTerminal の画面に手を入れる口は本体に無いので、**Mulmo Control が中継（34598）を立て、HTML にだけガイドを差し込む**形にした。試作でシェルの往復（`PROXY_42`）まで確かめてから組んだ。**書こうとしたところで止められて、試作の穴に気づいた** — 上流に弾かれないよう Origin を書き換えていたが、門番をしていなかったので、ブラウザで開いた**どのページからでもシェルを握れる**形だった。中継が自分で Host / Origin を確かめてから書き換える形に直し、よそのサイトを名乗る接続が断られることを `check.sh` で実際に投げて確かめている（166）。中継は「開く」を押したときだけ立て、立たなければ直接開く
- 2026-09-20: **#209**。#207 を配った直後、「ガイドが出ない」と報告が来た。中継は立っていて配れてもいたが、**見ていた窓が違った** — 中継はアドレスが変わるので窓が2つになり、前から開いていた `localhost:34567` の窓を見たままだった。作った側には自明でも、押した人には窓が増えたことしか見えない。行に「いまどこで出ているか」（`127.0.0.1:34598 の画面で出ています`）を出し、「開く」で中継を使ったときは**両方のアドレスを書いた知らせ**を起動ごとに1回だけ出すようにした。ガード170
- 2026-09-20: **#210 の続き**。行の判定が「自分が立てた子が生きているか」だったので、**アプリを更新した直後だけ行が嘘になる**（新しいアプリが起動しても、前のアプリが立てた中継は残るため）。v1.0.73 へ更新した直後に実際にそうなった。判定をポートが答えるかに変えた（`portIsOpen` は MulmoTerminal の判定で毎回の巡回が既に使っているので通信は増えない）。ガード170に1行追加
- 2026-09-23: **#212**。スマホ連携（Claude Code）の1部屋が、**Mac の再起動や異常終了で消えたら Mulmo Control が立て直す**ようにした。`繋ぐ` で希望を覚え（`claude-remote-want`）、`止める` で忘れる。`mulmo-claude-remote ensure` は希望あり・死んでいる・信頼済みのときだけ立て、未信頼ならターミナルも開かず何もしない。30分に3回落ちたら止めて「繋ぐ」で再開。呼ぶのはアプリ起動時・パネルを開いたとき・5分おきの巡回。ガード171〜176 は**本物のスクリプトを偽の HOME と偽の claude で実際に走らせて**見る。ついでに、再起動で pid が使い回されると「生きている」と誤読する穴（控えた番号だけで判定していた）を塞いだ。実機の再起動は未確認
- 2026-09-24: **#214**。環境タブのスマホ連携に「リレー」の行を足した（`relay` コマンドがある人にだけ出る）。session-relay の受け口と Cloudflare Tunnel が落ちていたら、`relay doctor --fix` で起こして行の下に「トンネルが止まっていたので 9/24 1:40 に起こしました」と出す。見る・直すの中身は relay 側（`relay doctor --json` / `--fix`、session-relay で同日追加）、こちらは呼ぶ回数と控えだけ。30分に3回で自動をやめ、「直す」で再開。起こしても直らない失敗（登録漏れ）には `--fix` を呼ばない。`--json` を知らない古い relay は「更新が要ります」に落とす。トンネルの生死は launchctl の状態で見る（Access の 401 はトンネルが止まっていても返る。relay 側で実測）。ガード178〜184 は本物のスクリプトを偽の HOME と偽の relay で走らせる
- 2026-09-24: **#216**。session-relay の `relay doctor` がトンネルの接続の本数（cloudflared の `/ready`、id `tunnel-ready`）を見るようになったので、リレーの行の直せる項目に足した。常駐は動いていて外からも 401 が返るのに接続だけ0本、という一番捕まえたい形で起こさなかった。未確認（本数を決められない）では起こさない。ガード185・186
- open は3本。**#6**（作者以外の環境で通っていない）**#23**（スマホ連携の再接続）**#67**（検査カタログ）。いずれも本人の手か別のマシン、あるいは「本当に切れる瞬間」が要る

## ⏭ 残り

- GitHub の Issues 一覧が正。ここには書き写さない

## ⚠️ ハマりどころメモ

- アプリの入手経路が2つある（Releases ダウンロード / 手元ビルド）。テストは両方通すこと（MULMO_CONTROL_ZIP_URL で失敗を再現できる）
- クリーンルーム検証: 空の HOME を作って `env HOME=... ./install.sh`（/Applications への書き込みだけ sed で退避先に差し替える）
- 旧 Air の npm は ECONNRESET が出やすい（回線起因、再実行で通る）
- **文字列で見る検査には天井がある。** 行番号や綴りを見るガードは「書いてあるか」しか言えず、「正しく振る舞うか」は言えない。純粋な関数に切り出せば動かせる（#149）。切り出す条件は、外の世界に触らないこと
- **「動いているか」を他人のプロセスで判定しない。** #152 で `claude agents --json` を使ったら、同じフォルダの別セッションを拾って「止めたのに動作中」と出た。自分が立てたものの pid だけを見る
- **片側だけ直した修正は、ガードも片側にしか掛からない。** #147 がまさにそれで、#145 で塞いだ穴の対になる箇所が半月ほど開いたままだった。対になるスクリプトがあるときは、ガードをファイル名で書かずに**列挙して回す**
- **リリースは手で組まない。`./release.sh` を使う。** v1.0.9 は配信物を確かめずに出して、ほとんどのボタンが動かない版を配った。script には配る前後の確認が入っている
- **アプリのパスには空白がある**（`/Applications/Mulmo Control.app`）。コマンド文字列にパスを埋めるときは必ず `tool()` / `bin()` を通す。裸で埋めると `/Applications/Mulmo` で切れる
- **`.buttonStyle(.plain)` の当たり判定は描画された中身に従う。** 背景が透明なボタンは文字の上でしか押せないので `contentShape` を付ける
- **同梱スクリプトは署名に封入される**（`Sealed Resources ... files=23`）。`scripts/` の中身を1バイトでも書き換えると `codesign --verify` が落ちるので、配布物のスクリプトを手で直さない。直すならソースを直して `build-app.sh` からやり直す
- **積んだ PR を `--delete-branch` でマージしない。** 土台のブランチが消えた瞬間、そこを向いている PR は GitHub に CLOSED にされる。閉じた PR は base を変更できないので、その場では直せない。2026-08-25 に #140 → #142 → #144 を積んだとき実際に踏んだ。下から順にマージし、1本ごとに上の PR の base を `gh pr edit --base main` で向け直し、squash で変わった SHA を `git rebase --onto` で落とす。ブランチの削除は全部終わってから
- **作者の Mac がライトモードなら、暗いときの画面は一度も出ない。** #143 はまさにそれで、`Palette` の8色が明るいとき用の決め打ちだった一方、台紙だけ `.ultraThinMaterial` で外観についていくため、夜は文字だけが取り残されていた。補助文字のコントラスト比は **1.00:1**（背景と完全に同じ明るさ）。**目で気づくことに頼れない種類の不具合なので、`check.sh` 側に落とす**（108番）。#6 と同じ穴
- **このアプリが持っている環境変数は、起こす相手に全部伝わる。** MulmoTerminal のセルから Mulmo Control を起動すると `PORT=34567`（MulmoTerminal のポート）を持つ。#141 はそれが `yarn dev` まで素通りして、MulmoClaude のバックエンドが他人のポートを掴みにいっていた。厄介なのは **vite だけ数秒立つので生存判定が「動作中」と言う**こと。中身が空なだけで、直ったように見える
- **`security add-generic-password -w` に値を stdin で流すと 128 バイトで黙って切り捨てられる。** 2100 バイトの blob が 128 バイトになり、エラーは出ない。読み戻して初めて分かる。引数で渡すしかなく、その代わり同じ利用者の `ps` から一瞬見える。**防げていないことは `SECURITY.md` にそう書く**（#145）
- **未コミットの変更がある状態で `git checkout -- <path>` を後始末に使わない。** 「ガードを壊して落ちることを確かめる」検査の restore に使ったところ、まだコミットしていなかった実装ごと消えた。**先にコミットしてから壊す**（#145 の作業中に実際に踏んだ）
- **公開リポジトリに個人名を書かない。** 誰が何に困ったかは、本人の許可なく出してよい情報ではない。**利用者の報告を扱うときが一番危ない** — 会話に出てきた名前をそのまま Issue や PR に書いてしまう。しかも `check.sh` はリポジトリの中しか見ないので、**Issue と PR の本文は最初から視界の外**。ガードが無かった間に3箇所（START-HERE / KICKOFF / LEARNING-LOG）が全リリースを通り抜けていた。守れていたつもりですらなく、一度も見ていなかった（152番）
- **上流が「埋まっていたら次のポートへ逃げる」作りなら、こちらの決め打ちは必ずいつか外れる。** 逃げる動作自体は止められないが、**実際に待っている場所を読む**ことはできる（#190）。プロセスが分かれば `lsof` で引ける。ただし画面からは叩けないので、スクリプトが控えを書く役になる
- **開発モードの2本（画面側とバックエンド）は、番号では見分けられない。** どちらも逃げるため。**プロセスの綴りで決める。** 取り違えると `開く` が API を、ブリッジが画面側を掴む（#190）
- **画面に出す文に Markdown を書かない。** SwiftUI の `Text` は解釈せず**そのまま記号として出す。** バッククォートも `**` も見えてしまう。強調したいときは「」で囲む（#192）
- **行を1つ足すたびに伸びる画面は、いつか必ずはみ出す。** そして最初に押せなくなるのは**一番下にある物**（このアプリでは `終了`）。#192 で効いていたのは、増えた行そのものより**増えたカードの器**だった。カードは1枚ごとに余白 28pt と間隔 15pt を食う。押すだけ・見るだけの物に1枚ずつ台紙は要らない
- **メニューバーのパネルをスクロールで解決しない。** 最初そう直そうとして却下された。あの大きさの面は、スクロールが出た時点で作りが間違っている（#192）
- **Secret を `run:` の中に `${{ secrets.X }}` と直接書かない。** シェルの文として展開されるので、エラー文や `set -x` に丸ごと出る。`env:` で環境変数にしてから使う。GitHub のマスクは「値がそのまま出た」ときだけ効く（#198）
- **入口は1つにする。** リリースを「Actions のボタン」と「タグ push」の両方から出せるようにすると、いつか両方から出る。#187 の連打が二重起動になったのと同じ形（#198）
- **「動いていないこと」をポートで判定できるのは、ポートが立つまでの時間が0のときだけ。** #187 はそこが1分あった。ビルドの間、画面は「停止中」と表示し、起動ボタンは何度でも押せる。**押せるのに効かないボタンは、連打の入口になる。** 立つ前から立ったあとまで持ち続けられる物（錠）が要る
- **上流が「ポートが埋まっていたら次の番号へ逃げる」作りなら、ポートで身元は決まらない。** MulmoClaude は 3002, 3003… へ逃げる。逃げた先は誰も見ていないので、孤児が通知を送り続ける
- **プロセスで身元を見るときは、綴りが「どこかに出てくる」だけで拾ってはいけない。** `zsh -lc "... yarn server ..."` のような包みが混ざる。作業ディレクトリが合っていれば自分のものに見えるので、停止がそのシェルを落とす。**動かしている本体が node / yarn / npm であること**まで見る（#187 の検査の足場を組んだとき、実際に3本拾った）
- **`grep -q` で書いたガードは「どれか1本が通れば合格」。** 起こす行が1本のうちに書いたガードは、行が増えた日に効かなくなる。#187 で Telegram ブリッジの nohup を足したとき、PORT のガード（107）がまさにその形だった。本数を数えて突き合わせる
- **競り合いは、逐次の検査では見えない。** 「2本目の acquire が断られる」だけでは、読んでから書く錠（原子的でない錠）が通る。**同時に5本取りに行かせて、勝つのが1本だけであること**まで見る（#187 で実測: 読んでから書く錠は5本全部通った）
- **自己更新はバンドルの外に逃げてから走る**。`install.sh` が `/Applications/Mulmo Control.app` を `rm -rf` する一方、`mulmo-control-self-update` はその中から実行されるため。zsh はスクリプトを少しずつ読むので、消えたファイルの続きを読むと途中で死ぬ（`MULMO_SELF_UPDATE_DETACHED` で一度だけ再 exec している）
