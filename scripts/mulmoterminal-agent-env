#!/bin/zsh
# LaunchAgent まわりの共有定義。実行するものではなく source して使う。
#
# ラベルとポートが5箇所に散らばっていて、変えるときに拾い漏れる形だった
# （Issue #7）。ここ1箇所にまとめてある。
#
# 名前が mulmoterminal- で始まっているのは意図的で、install.sh が
# scripts/mulmoterminal-* を ~/.local/bin へ配るときに一緒に付いてくる。
# LaunchAgent から起動される側もこの定義を読める必要がある。

# 配るものの識別子に個人名が入っているのは不自然なので、アプリの名前にした
# （Issue #7）。他人の Mac にも com.shutanuma.* が残る形だった。
MULMO_AGENT_LABEL="${MULMOTERMINAL_AGENT_LABEL:-com.mulmocontrol.mulmoterminal}"

# 古いラベルで入っているものは install-agent が片付ける。ここに並べておけば、
# 将来また変えるときも1行足すだけで済む。
#
# 環境変数で差し替えられるのは、移行の確認を本番の Agent を止めずに行うため。
# 実ラベルのまま試すと、確認の副作用で動いているものを外してしまう。
MULMO_AGENT_LEGACY_LABELS="${MULMOTERMINAL_LEGACY_LABELS:-com.shutanuma.mulmoterminal}"

# 既に使われている場合の逃げ道として環境変数で上書きできる。
MULMO_AGENT_PORT="${MULMOTERMINAL_PORT:-34567}"
MULMO_AGENT_HOST="${MULMOTERMINAL_HOST:-127.0.0.1}"

MULMO_AGENT_PLIST="${HOME}/Library/LaunchAgents/${MULMO_AGENT_LABEL}.plist"
MULMO_AGENT_DOMAIN="gui/$(id -u)"
MULMO_AGENT_LOG_DIR="${HOME}/.mulmoterminal/logs"

# Mulmo Control が預かる「スマホ連携（MulmoClaude）の鍵」の置き場所（Issue #145）。
#
# MulmoClaude を止めると、接続に使う session blob はホスト側から消える。
# ブラウザの localStorage にも同じものが park されているが、MulmoClaude の画面を
# 開いていない間は繋ぎ直す主体がいない。だから Mulmo Control が控えを持つ。
#
# 書く側（mulmo-check-remote-host）・読む側（mulmoclaude-remote-host-reconnect）・
# 消す側（同上と uninstall.sh）が別のファイルなので、綴りが食い違うと「預けたのに
# 読めない」形になる。#129 で実際に踏んだ壊れ方なので、ここ1箇所が持つ。
MULMO_MC_SESSION_SERVICE="${MULMO_MC_SESSION_SERVICE:-mulmo-control-remote-host-mulmoclaude}"
MULMO_MC_SESSION_ACCOUNT="${MULMO_MC_SESSION_ACCOUNT:-session}"
