#!/bin/zsh
set -eu

# 兄弟スクリプトは自分と同じディレクトリから呼ぶ。アプリに同梱された写しから
# 実行されても、~/.local/bin の写しから実行されても、同じ組み合わせで動く
# （Issue #51）。~/.local/bin は install.sh を通さないと存在しない。
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"

"${SCRIPT_DIR}/mulmoterminal-stop" || true
sleep 1
"${SCRIPT_DIR}/mulmoterminal-start"
