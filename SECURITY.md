# SECURITY.md

Mulmo Control の検査カタログ。出典は Issue #67。

このファイルは**現状の申告**であって、達成の宣言ではない。印が付いていない項目は
「まだ見ていない」という意味で、安全だと分かっているという意味ではない。

## 印の意味

| 印 | 意味 |
|---|---|
| ✅ | `check.sh` が自動で見ている。壊すと PR で落ちる |
| 🔨 | 実装はあるが自動検査は無い。人が変えれば黙って壊れる |
| ⛔ | 項目自体が無効になった |
| （空欄） | 未着手 |

## gate の分け方

| gate | いつ走るか |
|---|---|
| fast | PR ごと（`check.sh`） |
| release | 配る前（`release.sh` → `check.sh`） |
| manual | 人が触って確かめる |
| sandbox | 使い捨ての環境で走らせる |
| governance | 運用ルール。自動化しない |


## A. config/env parsing

| # | gate | 内容 | 状況 |
|---|---|---|---|
| 001 | fast | `app-info.env` が存在しない場合は既定値に落ちる | ✅ 設定は許可したキーだけを返す |
| 002 | fast | 空の `app-info.env` で落ちない | ✅ 同上 |
| 003 | fast | 正常な `MULMO_CONTROL_REPO_URL` を読める | ✅ 同上 |
| 004 | fast | 正常な `MULMO_CONTROL_SOURCE_DIR` を読める | ✅ 同上 |
| 005 | fast | 正常な `MULMO_CONTROL_BRANCH` を読める | ✅ 同上 |
| 006 | fast | 正常な `MULMO_CONTROL_MULMOCLAUDE_DIR` を読める | ✅ 同上 |
| 007 | fast | 未知のキーは無視する | ✅ 同上 |
| 008 | fast | `touch /tmp/...` のような行は実行されない | ✅ 設定を source していない |
| 009 | fast | `$(...)` を含む値/行は実行されない | ✅ 同上 |
| 010 | fast | backtick を含む値/行は実行されない | ✅ 同上 |

008〜010 の ✅ は、最初は**過大申告だった**（#83）。ガードが `"${CONFIG}"` という綴りだけを
探しており、`$CONFIG`・変数名違い・パス直書き・一行の `&&` は素通りしていた（5通り中4通り）。
現在は「`mulmoterminal-agent-env` 以外を `.` / `source` していたら落とす」という whitelist に
変えてあり、5通りすべてで落ちることを実測済み。**禁止したい綴りを並べる形では勝てない。**

## B. quoting/path safety

| # | gate | 内容 | 状況 |
|---|---|---|---|
| 011 | fast | パスに空白があっても command string が壊れない | ✅ 起動スクリプトは空白入りの場所を切らずに渡す |
| 012 | fast | パスに `'` があっても config parser が読める | ✅ 設定は許可したキーだけを返す |
| 013 | fast | MulmoClaude dir に空白があっても start script が壊れない | ✅ 起動スクリプトは空白入りの場所を切らずに渡す |
| 014 | fast | MulmoClaude dir に `'` があっても LaunchAgent plist が壊れない | ✅ plist は & < > ' を含む場所を往復できる |
| 015 | fast | `/Applications/Mulmo Control.app` を裸で shell に埋め込まない | ✅ tool()/bin() を通している |
| 016 | fast | `toolsDir` は Swift 側で helper 経由になる | ✅ 同上 |
| 017 | fast | `localBin` は Swift 側で helper 経由になる | ✅ 同上 |
| 018 | fast | XML plist 値に `&` / `<` / `>` が入っても escape される | ✅ 同上 |
| 019 | fast | log path に空白があっても tail/open が壊れない |  |
| 020 | fast | app bundle内の同梱scriptを空白入りpathから実行できる | ✅ 空白入りパスからの実行 |

## C. shell safety

| # | gate | 内容 | 状況 |
|---|---|---|---|
| 021 | fast | shell entrypoint は shebang を持つ | ✅ 全スクリプトにシェバンがある |
| 022 | fast | shell entrypoint は `set -u` または `set -eu` を持つ | ✅ set -u / set -eu がある |
| 023 | fast | `curl | sh` / `wget | sh` がない | ✅ 落としたものの丸投げ・eval・権限昇格がない |
| 024 | fast | `eval` がない | ✅ 同上 |
| 025 | fast | `sudo` がない | ✅ 同上 |
| 026 | fast | 許可していない `rm -rf` がない | ✅ rm -rf の対象は決まった3つだけ |
| 027 | fast | `rm -rf` は固定された安全な対象か temporary dir に限定される | ✅ 同上 |
| 028 | fast | script syntax を `zsh -n` で検査する | ✅ zsh -n による構文検査 |
| 029 | fast | background job による `nice(5)` 警告が検査出力に出ない |  |
| 030 | fast | helper script が同梱scripts数に含まれる |  |

## D. install/uninstall

| # | gate | 内容 | 状況 |
|---|---|---|---|
| 031 | release | macOS 14 未満で install が止まる |  |
| 032 | release | npm がない場合に install が明確に止まる |  |
| 033 | release | MulmoClaude がない場合も MulmoTerminal用途では install が続く |  |
| 034 | release | `MULMOCLAUDE_DIR` 指定が app-info.env に保存される |  |
| 035 | release | build済みzip取得成功時は local build に落ちない |  |
| 036 | release | zip取得失敗時は local build に fallback する |  |
| 037 | release | `MULMO_CONTROL_BUILD=1` で必ず local build する |  |
| 038 | release | install後に `/Applications/Mulmo Control.app` が存在する |  |
| 039 | manual | uninstall は app と LaunchAgent を消し、ログ/データは残す |  |
| 040 | manual | uninstall 後に再installできる |  |

## E. self-update

| # | gate | 内容 | 状況 |
|---|---|---|---|
| 041 | fast | self-update check は installed version を Info.plist から読む | ✅ ソースの HEAD ではなくアプリ自身から読んでいることを見る（#31） |
| 042 | fast | release tag が読めない時は `unknown` status を書く | ✅ 上流を確かめられないときは unknown と書く |
| 043 | fast | installed が latest と同じなら `current` | ✅ `mulmo-version-status` を**実際に動かして**10通りと突き合わせる |
| 044 | fast | installed が latest より新しければ `current` | ✅ 同上（手元ビルドがリリースより先の場合） |
| 045 | fast | installed が古ければ `update` | ✅ 同上。1.0.9 と 1.0.53 が**文字列比較で逆になる**ことも見る |
| 046 | fast | self-update apply は detached copy で動く |  |
| 047 | release | source repo が既存gitなら fetch/pullする |  |
| 048 | release | source repo がなければ cloneする |  |
| 049 | release | apply 失敗時に log が残る |  |
| 050 | manual | self-update 後にアプリを再起動できる |  |

## F. MulmoTerminal/LaunchAgent

| # | gate | 内容 | 状況 |
|---|---|---|---|
| 051 | fast | `mulmoterminal-agent-env` は sibling script として読まれる |  |
| 052 | fast | LaunchAgent plist は `plutil -lint` に通る | ✅ 同上 |
| 053 | fast | LaunchAgent WorkingDirectory がない時は `$HOME` に逃がす | ✅ 場所が無いときは HOME に逃がす |
| 054 | fast | locale fallback `LANG=C.UTF-8` が start script にある | ✅ 起動スクリプトのロケール補完 |
| 055 | fast | 既にport 34567が応答中なら二重起動しない | ✅ 起動前に二重起動を避けている |
| 056 | manual | 起動ボタンで LaunchAgent が bootstrap される |  |
| 057 | manual | 停止ボタンで LaunchAgent が bootout される |  |
| 058 | manual | 再起動ボタンで kickstart または再起動できる |  |
| 059 | manual | keepalive-enabled の作成/削除が期待通り | ⛔ keepalive-enabled は廃止した（表示専用で何も制御していなかった） |
| 060 | manual | 自動起動したMulmoTerminalで日本語が `_` にならない | 🔨 #62 で修正・実機で実測 |

## G. MulmoClaude repo states

| # | gate | 内容 | 状況 |
|---|---|---|---|
| 061 | fast | MulmoClaude dir が存在しない場合に status が分かる | ✅ 場所が無い・git repo でないときは止まる |
| 062 | fast | MulmoClaude dir がgit repoでない場合に update が止まる | ✅ 同上 |
| 063 | sandbox | dirty worktree は stash される | ✅ 退避を戻せずに壊れたら止まる |
| 064 | sandbox | stash 失敗時は update を中止して理由を残す | ✅ 退避に失敗したら、その先へ進まない |
| 065 | sandbox | default branch 以外なら default branch に戻す | ✅ 別のブランチを見ていたら既定のブランチに戻す |
| 066 | sandbox | checkout 失敗時は stash を戻す | ✅ 既定のブランチに戻せなければ退避を戻してから止まる |
| 067 | sandbox | pull失敗時は stash を戻す | ✅ pull に失敗したら退避を戻してから止まる |
| 068 | sandbox | pullで変化なしなら「すでに最新」理由を残す | ✅ 変化が無いときは「すでに最新」と言う |
| 069 | sandbox | yarn install 失敗時は理由を残す | ✅ yarn install に失敗したら理由を残す（手元のみ。CI の yarn は壊れた package.json でも成功するので飛ばす） |
| 070 | manual | update後にMulmoClaudeを再起動する |  |
| 187 | sandbox | 退避を戻すときにロックファイル（`yarn.lock` / `package-lock.json` / `pnpm-lock.yaml`）だけがぶつかったら、更新後の側を採って先へ進む | ✅ 使い捨ての git リポジトリで `yarn.lock` だけがぶつかる形を作って更新を走らせ、未解決が残らず・止まらず・ぶつからない本人の変更は戻り・stash は消えないことを見る。止めると未解決のロックが残り、次の更新が退避から落ちる（#218） |
| 188 | sandbox | 前回の更新でぶつかったまま残ったロックファイルがあっても、更新が通る | ✅ stash pop が `yarn.lock` でぶつかった後の形を git で作って更新を走らせ、ロックがそろって上流の次のコミットまで届くことを見る。187 が入る前に壊れた Mac はこの形で止まっている。実際に4回続けて「退避できなかった」で止まった（#218） |
| 189 | sandbox | ロック以外がぶつかったまま残っていたら、名前を出して止まり、そのファイルには触らない | ✅ `CLAUDE.md` が未解決のまま残った形で更新を走らせ、失敗として終わり、理由にファイル名が出て、中身・未解決の状態・stash がそのままであることを見る。以前は「退避できなかった」とだけ出て、何がぶつかっているのか分からなかった（#218） |

## H. npm/network/update checks

| # | gate | 内容 | 状況 |
|---|---|---|---|
| 071 | fast | node がない時に update cache が `Node未検出` になる | ✅ node が無いときは「Node未検出」と言う |
| 072 | fast | npm がない時に npmLatest が `unknown` になる | ✅ npm を外した使い捨て環境で実際に走らせる |
| 073 | fast | npm latest timeout で落ちない |  |
| 074 | fast | MulmoTerminal version parse が SemVer を拾う |  |
| 075 | fast | package.json workspace package version を拾う |  |
| 076 | fast | dependency version の `^` / `~` を剥がす |  |
| 077 | fast | unknown がある時 summary が `一部未確認` | ✅ npm が答えない環境で summary を見る |
| 078 | fast | update がある時 summary が件数を出す | ✅ 件数が items の実数と合うかまで見る |
| 079 | release | `mulmo-npm-install` は一時的な失敗を1回だけ retry する | ✅ 呼び出しが2回・待ちがあることを見る |
| 080 | release | npm cache は `/private/tmp` 側を使う | ✅ 既定値の綴りを見る |

## I. logs/status JSON

| # | gate | 内容 | 状況 |
|---|---|---|---|
| 081 | fast | self-update status JSON が valid JSON | ✅ 同上 |
| 082 | fast | mulmo updates cache が valid JSON | ✅ 更新の一覧は形を保つ |
| 083 | fast | update reasons が tab-separated で読める | ✅ 更新の理由はタブ区切りで読める |
| 084 | fast | last update summary が存在しない時もUIが落ちない | ✅ 更新していない人には要約ファイルを作らない |
| 085 | fast | legacy log dir から新log dirへの逆戻りがない | ✅ ~/Documents/Codex を参照していない |
| 086 | fast | logs path は `~/Library/Logs/Mulmo Control` に揃う | ✅ ログの置き場所は1つだけ |
| 087 | manual | ログボタンで正しいログ場所を開く |  |
| 088 | manual | status UI に `unknown/current/update/missing` が出る |  |
| 089 | manual | 通知済みkeyで同じ通知を繰り返さない |  |
| 090 | manual | app self-update通知とtool update通知が混ざらない |  |

## J. release/artifact/AI governance

| # | gate | 内容 | 状況 |
|---|---|---|---|
| 091 | fast | Info.plist と package.json の version が一致する | ✅ Info.plist と package.json の一致 |
| 092 | fast | `bin/mulmo-control.mjs` が `node --check` に通る | ✅ bin/mulmo-control.mjs の構文 |
| 093 | release | app bundle の scripts 数が source と一致する | ✅ 同梱スクリプト数 |
| 094 | release | codesign verify がクリーンコピーで通る | ✅ codesign verify |
| 095 | release | zip はクリーンコピーから作る |  |
| 096 | release | release後に latest zip を取り直してversion確認する | ✅ 取り直しと版の比較の両方を見る |
| 097 | governance | AI reviewer は最初 shadow mode でコメントのみ |  |
| 098 | governance | AIが作業開始時にIssueへ方針コメントを残す | 🔨 #67 に方針コメントを残した（当初は守れていなかった） |
| 099 | governance | 自動承認する場合は一定割合を人間がsample reviewする |  |
| 100 | governance | 新しいbug classが見つかったら `SECURITY.md` / `check.sh` に戻す | 🔨 #83 で1周した（ガードの穴 → Issue → 検査 → ここへ反映） |
| 101 | fast | 自己更新は、写しに連れて行かない兄弟スクリプトを使わない | ✅ 参照と cp の対象を突き合わせる |
| 102 | fast | 追加ツールを入れる場所と、版を読む場所が同じ | ✅ 綴りを突き合わせる（#129 で実際に食い違った） |
| 103 | fast | 更新一覧の項目は、どれかのボタンで更新できる | ✅ 一覧と追加ツールの id を突き合わせる（#131） |
| 104 | fast | 古い置き場所の引き継ぎは一度きり（毎起動で ~/Documents を読まない） | ✅ 印の guard があることを見る（#134） |
| 105 | fast | 「前回の更新」に残った失敗は、解消したら解消したと添える | ✅ 添える側が消えていないかを見る（#136） |
| 106 | fast | 停止中のサービスに、起動する手段が画面から見える | ✅ ServicePanel の中で startAction が2箇所から呼ばれることを見る（#139） |
| 107 | fast | 起こす MulmoClaude に、呼び出し元の PORT を引き継がせない | ✅ 起こす行**全部**が PORT を外していることを見る（#141）。以前は `grep -q` でどれか1本通れば合格だったので、#187 で足した Telegram ブリッジの行は素通りできた |
| 108 | fast | 配色が、明るいとき/暗いときの外観に応じて変わる | ✅ Palette の各行が adaptive( を通っていることを見る（#143） |
| 134 | fast | MulmoClaude を起こすモードで、待つポートと開く URL が2箇所で揃っている | ✅ シェルと Swift の対応表を突き合わせ、通常モードが 5173 を待たないこと・設定が無い/壊れているときに通常モードへ倒れること（#174 で向きを変えた）・production serve の2変数と `yarn dev` が消えていないことを見る（#168） |
| 135 | fast | MulmoClaude のモードが、ボタンを押さなくても画面から分かる | ✅ 1行目がモードを通っていることと、切り替えがボタンの列にあることを見る（#170） |
| 136 | fast | 「停止」で、起こし直す親ごと止まる | ✅ 起こし直す親を持つ偽の木を立て、**本物の `mulmoclaude-stop` を当てて**、親が死んでいることを見る（#172） |
| 137 | fast | 身元の判定が、symlink 越しの置き場所でも一致する | ✅ 同上（検査の木は `/var`→`/private/var` の下に立つ。一致しないと停止も切り替えも届かない） |
| 138 | fast | 既定を変えたことを、選んでいない人に一度だけ知らせる | ✅ 知らせる口があること・呼ばれていること・自分で選んだ人には出さないことを見る（#174） |
| 139 | fast | リリースノートが取れない日でも、GitHub へ飛ぶ口が残る | ✅ 届かない URL で実際に走らせて、0 で終わり控えを壊さないことと、控えが無いときの表示と飛ぶ口が画面にあることを見る（#176） |
| 140 | fast | リリースノートが、何のものか分かる形で出る | ✅ 見出しにアプリ名が入っていることと、本文を素通しで描いていないことを見る（#178） |
| 141 | fast | 更新の帯の説明が、ボタンに押されて切れない | ✅ 説明を1行に切り詰める指定が戻っていないことを見る（#180。捨てられるのは「何が更新されるか」の一覧） |

## K. スマホ連携の鍵

出典は Issue #145 と #154。Mulmo Control は「スマホ連携」の session blob を
Keychain に預かる。中身は Firebase の refreshToken なので、扱いを間違えると
**平文でログに残る／アンインストールしても残る**という形になる。

預かる先は**2つある**。MulmoClaude 側（#145）と MulmoTerminal 側（#154）で、
同じ Google アカウントを使うが接続は別々なので blob も別。#145 のときに
MulmoTerminal 側を入れ忘れ、同じ「まだ繋いでいません」に対して片方だけが
「繋ぐ」を出し、もう片方は設定画面へ送っていた（#154 で実測）。

| # | gate | 内容 | 状況 |
|---|---|---|---|
| 109 | fast | 預かった鍵/トークンを `echo` / `tee` に渡さない（ログに平文で残る） | ✅ 繋ぎ直す2つのスクリプトと check を対象に、秘密を持つ変数が出力コマンドに渡っていないかを見る |
| 110 | fast | 鍵の置き場所（service / account）の綴りを共有定義だけが持つ | ✅ 直書きを禁じ、Keychain を触る箇所が消えていないかを見る |
| 111 | fast | 401（期限切れ）と `uninstall.sh` の両方に鍵を捨てる口がある | ✅ **繋ぎ直す2本とも**と、`uninstall.sh` が**2つとも**消していることを見る |
| 112 | fast | 繋がっているときに reconnect を叩かない（#23 / #147 で 401 を実測） | ✅ **両方**の繋ぎ直しスクリプトで、引き返す判定が POST より前にあることを行番号で見る |
| 113 | manual | 預かった鍵を読むときに Keychain の許可ダイアログが出ない | |
| 114 | manual | MulmoClaude を止めた状態から `繋ぐ` の1押しで緑に戻る | 🔨 2026-08-28 に実測（14.6秒・ブラウザ無し） |
| 115 | fast | 状態の全組み合わせで、スマホ連携の行に出る言葉が対応表と一致する | ✅ `StatusDisplay.swift` と検査を `swiftc` で組んで**実際に走らせる**（12通り） |
| 126 | fast | 預かる先の名前が2つで分かれている（同じだと後から預けたほうが先を潰す） | ✅ 共有定義の2つの service 名が違うことを見る |

126 がここ（K 節）にあって L 節（116〜125）より後ろなのは、#152 と並行で
作ったため。番号は付けた順であって、節の順ではない。

## L. エージェントのスマホ連携（Issue #160、もとは #152）

Claude Code と Codex のリモート機能を、環境タブの行から繋ぐ。

2つは形が違う。**Claude Code はセッション1つ＝フォルダ1つ**で、立てると入口の
URL が出る。**Codex は Mac に1つのデーモン**で、URL は無く、代わりにペアリング
コードで端末を繋ぐ。

171〜176 は Claude Code 側の**自動の繋ぎ直し**（#212）。「繋ぐ」を押すと希望を
覚え、Mac の再起動や異常終了で消えたら Mulmo Control が立て直す。押すのは人、戻すのは
機械で、**止めたものと未信頼のフォルダには手を出さない。**

| # | gate | 何を守るか | どう見るか |
|---|---|---|---|
| 116 | fast | `claude` の信頼フラグ（`hasTrustDialogAccepted`）を書かない／確かめてから立てる | ✅ 書き込みの形を禁じ、読む側が消えていないことと、確認が起動より前にあることを見る |
| 120 | fast | セッションの画面出力を保存しない（会話が平文で残る） | ✅ 行き先が `/dev/null` か FIFO のどちらかだけであることを見る |
| 121 | fast | 止めるのは自分が立てたものだけ（人が開いた作業中のものを消さない） | ✅ `pkill` / `killall` を禁じ、控えた pid で止めていることを見る |
| 128 | fast | FIFO から拾うのは入口の URL だけ（それ以外を書けば 120 が素通りする） | ✅ 書く口が1つだけで、一致した文字列しか渡らないことを見る |
| 129 | fast | ペアリングコードをログに残さない（1つで端末が繋がる） | ✅ `CODE` が出力コマンドに渡っていないことを見る |
| 130 | manual | 立てたセッションが、スマホから実際に開ける | |
| 131 | fast | 行に出す説明を、入る長さで書く（潰れるとその行の情報が消える） | ✅ スクリプトが書く説明の文字数を数える（12文字まで） |
| 132 | fast | 枠が埋まっているときに繋ぎに行かない（409 の再接続を回し続ける） | ✅ 確かめる行が `start` より前にあることと、畳む口があることを見る |
| 133 | fast | 行のボタンを画面側で継ぎ足さない（対応表を通らない分岐ができる） | ✅ `buttonTitle:` に `??` が無いことを見る |
| 171 | fast | 「止める」を押したものは、自動で立て直さない | ✅ 本物の `mulmo-claude-remote` を偽の HOME で走らせ、stop のあと希望が消えていて、ensure しても立たないことを見る。希望を消す行が kill より前にあることも見る（止めている途中に ensure が走っても戻さない）（#212） |
| 172 | fast | 未信頼のフォルダでは ensure は何もしない（ターミナルを開かない・信頼に触れない） | ✅ 同じ偽の HOME で信頼を外して ensure し、立たずに `untrusted` と言うことを見る。ターミナルは走らせると本当に開くので、ensure の中に `osascript` / `do_start` / `claude.json` が無いことと、信頼の確認が立てる行より前にあることで見る（#212） |
| 173 | fast | 繋いでおきたいセッションが落ちたら立て直し、落ちたことを状態に残す | ✅ 偽の claude で立て、控えた pid を落として ensure し、別の pid で立ち直ることと `event=restored` を見る（#212） |
| 174 | fast | 短時間に何度も落ちるなら立て直しをやめ、「繋ぐ」で再開できる | ✅ 上限（`RESTART_LIMIT`、定数）まで落として立て直させ、次の1回で `halted` になり立たないこと・もう一度 ensure しても立たないこと・start で `online` に戻り記録が消えることを見る（#212） |
| 175 | fast | 希望の置き場所が画面とスクリプトで揃い、起動時と巡回の両方から ensure を呼ぶ | ✅ 置き場所の綴りを突き合わせ、`init()` と `refresh()` と軽い確認の3か所から呼んでいること、希望の有無を見てから呼ぶこと（#38）を見る。食い違っても何も落ちず、再起動のあと戻らないだけなので気づけない（#212） |
| 176 | fast | 自動の繋ぎ直しの知らせが、状態ごとに対応表どおり出る | ✅ `claudeRemoteNotes` を `StatusDisplay.swift` ごと組んで11通り**実際に走らせる**（止めた人には何も言わない・未信頼・ログイン時に起動しないときの注意を含む）。画面がその1本を通っていることも見る（#212） |
| 177 | fast | 落ちてから自動で繋ぎ直すまでの間は「落ちました」と言い、止めたあとは言わない | ✅ 偽の claude で立てて落とし、status が `crashed` になることを見る。stop のあとは `crashed` にならないことも見る。「まだ繋いでいません」のままだと、一度も繋いでいないように読める（#212。実機で試して気づいた） |

## M. 連打で二重に立ち上がらない（Issue #187）

利用者の実測: 33秒の間に `起動` を3回・`更新` を4回押したところ、`yarn server` が
2本（3001 と 3002）孤児として残り、翌朝の定期通知が4重に届いた。

効いていなかったのは2つ。**ポートを見るガードは、ポートが立つ前の1分を見られない**
（通常モードの起動は画面を作り直すのに約1分かかる）。そして **MulmoClaude はポートが
埋まっていると 3002, 3003… へ逃げる**ので、2本目以降はそのガードの視界に入らない。

ここは全部、綴りではなく**同じ形のプロセスを立てて実際に見る**。「プロセスも見ている」
ことを grep で確かめても、見方が間違っていれば通るため。

| # | gate | 何を守るか | どう見るか |
|---|---|---|---|
| 142 | fast | ポートを持っていない2本目を見つける（3002 へ逃げた分が孤児にならない） | ✅ 誰も持っていないポートを見させた状態で `node .../yarn.js server` を2本立て、両方が返ることを見る（ポート側では1本も見つからないので、見つかった分は全部プロセス側が拾ったことになる） |
| 143 | fast | 拾ってはいけないものを拾わない（停止が巻き添えにする） | ✅ 同じフォルダに `build:client`・別の node（人の claude セッション）・綴りを含む文字列を渡されたシェルを立て、どれも返らないことを見る |
| 144 | fast | 錠は一度に1本だけ（逐次でも、同時に押されても） | ✅ 2本目の acquire が断られることと、5本同時に取りに行って勝つのが1本だけであることを見る（逐次だけでは競り合いを見ていない） |
| 145 | fast | 置き忘れた錠は奪える（強制終了や電源断のあと永久に起動できなくならない） | ✅ 持ち主を KILL してから取り直せることを見る |
| 146 | fast | 起動・再起動・更新は錠を通り、**停止は通らない** | ✅ 3本を列挙して回し、acquire と引き渡し（`MULMO_MC_LOCK_HELD`）の両方があることを見る。停止側には無いことも見る（最後の逃げ道を塞がない） |
| 147 | fast | 処理中の起動は、理由を言って引き返す（立ち上げまで進まない） | ✅ 錠を持たせた状態で**本物の `mulmoclaude-start` を当てて**、理由が出ることと `started` まで進まないことを見る |
| 148 | fast | ビルド中に押された停止が、立ち上げの手前で効く | ✅ 印を見る行が立ち上げ行より前にあることと、停止側が印を残していることを見る（順番が入れ替わると、止めたはずの物が1分後に立つ） |
| 149 | fast | 画面の押す口はどれも、押した瞬間に錠を読み直す | ✅ `startMC` / `restartMC` / `updateMC` / `openMC` / `updateAllInstalled` / `setMCMode` / `setMCTelegram` を列挙して回す。`stopMC` は通っていないことも見る |
| 150 | fast | 錠を諦める時間が、画面とスクリプトで揃っている | ✅ `MAX_AGE` と `maxAge` を突き合わせる（食い違うと「画面は処理中と言うのにスクリプトは錠を奪う」になる） |
| 151 | fast | Telegram ブリッジ: 既定は off・停止で一緒に落ちる・接続先が表と揃う | ✅ 既定と知らない値が off へ倒れることを見て、停止が `--all` で取っていることと、`--api` がポートの表の最後と両モードで一致することを見る |
| 152 | fast | 公開リポジトリに個人名を書かない（誰が何に困ったかは、本人の許可なく出せない） | ✅ 人名に付く敬称を探す。名前を並べる形では次に出てくる名前に勝てないため。敬称の字は普通の語にも混ざるので、**許す語を先に消してから**、残った行だけを落とす。このガードが無かった間に3箇所すり抜けており、全リリースを通り抜けていた |
| 153 | fast | 3001 以外で立っていても、`開く` とブリッジの接続先がそこを指す | ✅ 借りた空きポートで**実際にプロセスを立てて**、`mulmoclaude-url` と `--api` がそこを返すことを見る。#187 では見つけて止められるようにしたが、アドレスは決め打ちのままだった |
| 154 | fast | 開発モードで、画面側とバックエンドを取り違えない | ✅ 画面側を子として持つ木を立てて、`開く` が画面側・`--api` がバックエンドを指すことを見る。番号では決められない（どちらも逃げる）ので、プロセスの綴りで決めている |
| 155 | fast | 実際のポートの控えが、書く側と読む側で揃っている | ✅ 置き場所の綴りを突き合わせ、停止が捨てていることを見る。画面は `lsof` を叩けない（1回 210ms・#38）ので控えを読む。食い違うと画面だけ既定のままで、落ちないので誰も気づけない |
| 156 | fast | 押すだけ・見るだけの物に、台紙を1枚ずつ立てない | ✅ 運用タブが `SettingsGroup` を通っていることと、中に入る行（トグル2つ・追加ツール）が自前の台紙を持っていないことを列挙して見る。カードは1枚ごとに余白 28pt と間隔 15pt を食う。#187 で行を1つ足したとき画面からはみ出したのは、増えた行より増えた器のほうが効いていた |
| 157 | manual | 画面が、13インチのノートでも収まる | |
| 158 | fast | 画面に出す文に記号（バッククォート）を紛れさせない | ✅ Swift の文字列に混ざっていないことを見る。Markdown のつもりで書いても Text はそのまま出す。ガードを入れた時点で、別の場所に1件すり抜けていた |
| 163 | fast | 「前回の更新」の詳細が、パネルの高さを押し広げない | ✅ パネルが記録を直に描いていないことと、押して開く口・高さの上限・あらましを出す1本を通っていることを見る。あらましは `StatusDisplay.swift` 側に切り出して6通りを**実際に走らせる**。記録は長い日で20行を超え、素で置くと一番下の「終了」が押せなくなる（#183。156 と同じ壊れ方） |
| 164 | fast | 画面ガイドの本体がリポジトリに在り、接続先が Swift 側と揃っている | ✅ ファイルの存在と、`GuideServer.port` とスクリプトの接続先ポートの一致、MulmoTerminal のポートを決め打ちしていないことを見る。前の版はどこにも置かれていなかった（#202 では「Tampermonkey ごと消えた」と書いたが誤りで、Tampermonkey は最初から入っていなかった・#207）。ポートが食い違っても何も落ちず、ガイドが出なくなるだけなので気づけない |
| 165 | fast | 更新直後の知らせも、長いときはパネルを押し広げない | ✅ `NoticeCard` が「前回の更新」と同じあらまし（`lastUpdateDigest`）を通り、開く口と高さの上限を持つことを見る。1件だけ動いた日はその行をそのまま出す形も含めて、あらましを8通り**実際に走らせる**。#183 で環境タブは畳んだが、更新直後は上部の知らせが同じ記録を全部広げていた（#205） |
| 166 | fast | 画面ガイドの中継が、よそのサイトからの接続を断る（MulmoTerminal の門番を素通りさせない） | ✅ 中継を**偽の MulmoTerminal に当てて実際に走らせ**、よそのサイトを名乗る WebSocket・HTTP と、Host を偽った要求（DNS rebinding）が 403 で断られ、上流まで渡らないことを見る。自分の画面からの WebSocket は往復し、上流には Host / Origin が書き換わって届くこと、HTML にだけ差し込み（圧縮されていても）、API は1バイトも変えないことも同じ検査で見る。**試作では門番なしで Origin を書き換えていて、ブラウザで開いたどのページからでもシェルを握れる形だった**（#207。書こうとしたところで止められて気づいた） |
| 167 | fast | 画面ガイドの中継はループバックだけで待ち受ける | ✅ 上の検査で LAN のアドレスから入れないことを見る。LAN のアドレスが無いマシンでは飛ぶので、`server.listen(LISTEN, LOOPBACK` の書き方と、全体で待ち受ける指定が無いことも見る |
| 168 | fast | 中継のポートが Swift と揃っている | ✅ `GuideProxy.port` と中継の既定 `GUIDE_LISTEN` を突き合わせる。食い違うと「開く」が答えない場所を開く |
| 169 | fast | 中継が立たないときは MulmoTerminal を直接開く | ✅ 開く先を決める関数に、オフのときと立たなかったときの `mtURL` への道があることを見る。中継が無いと本体が開けない形にしない |
| 170 | fast | 画面ガイドがどの窓で出ているかが、画面から分かる | ✅ 行が `GuideProxy.port` と中継の生死を見ていること、「開く」が**両方のアドレスを書いた知らせ**を1回だけ出すことを見る。中継はアドレスが変わるので窓が2つになり、前から開いていた窓を見たまま「出ない」と読める（#209。#207 を配った直後に実際にそうなった） |

## N. リリースを GitHub 側で回す（Issue #198）

リリースは `./release.sh` の1コマンドだったが、**手元の Mac で 5〜10 分見張る**必要が
あった（公証は Apple 側の審査で数分待つ）。1日に3回出した日があり、そのたびに端末の
前で待つことになったので、Actions の画面からボタン1回で出せるようにした。

預けるのは署名の証明書（.p12）と公証の資格情報（Apple ID・Team ID・App用パスワード）。
GitHub Secrets は暗号化保存で、実行中だけ使え、ログに出ても自動でマスクされる。
Apple 側でいつでも無効化できる。**リポジトリに書き込み権限がある人はリリースを走らせ
られる**ので、コラボレーターを増やすときは Environment の承認を足す（未着手）。

| # | gate | 内容 | 状況 |
|---|---|---|---|
| 159 | fast | リリースの入口は手動だけ・同時に押されても1本ずつ・手順は release.sh に任せる・終わりに一時キーチェーンを消す | ✅ `on:` 直下の鍵を列挙して `workflow_dispatch` 以外があれば落とす（入口が2つあると二重に出る。#187 の連打と同じ形）。`gh release create` / `notarytool submit` / `stapler` / `ditto -c` を自前でやっていたら落とす（2箇所に書くと片方だけ直す）。`if: always()` の step に `security delete-keychain` があることを見る。**コメントを落としてから探す**（語で探すと自分のコメントに当たる） |
| 160 | fast | ワークフローが読む Secret と、`release-setup.sh` が預ける Secret が同じ集合 | ✅ 両方から名前を抜き出して突き合わせる。片方だけ増やすと、押しても落ちる。しかも落ちるのは公証の直前。**名前に数字が入る**（`P12`）ので `[A-Z_]` では1つ取りこぼす。書いた直後に「4つ」と出て気づいた |
| 161 | fast | Secret は `env:` にだけ書く（`run:` に直接展開しない） | ✅ `secrets.` を含む行が全部 `KEY: ${{ secrets.KEY }}` の形であることを見る。`run:` に書くとシェルの文として展開され、エラー文や `set -x` に丸ごと出る |
| 162 | manual | 預けた資格情報で、実際に GitHub 側から1本出せる | 🔨 2026-09-11 に v1.0.69 で実測。押してから公開まで 1分45秒。署名は Developer ID・公証は Accepted・チケット添付・取り直しの確認まで通った。最初の2回は Secret が空で落ちた（`release-setup.sh` を走らせる前に押していた）。空なら最初の step で止まり、キーチェーンの後始末も走ることを、その2回で確かめた形になった |

## O. リレー（Issue #214 / #216）

session-relay（`relay` コマンド）でスマホの Claude から Mac へ会話を預けるには、受け口
（`relay mcp-deposit-http`）と Cloudflare Tunnel の2つの常駐が要る。Tunnel が exit 0 で
止まり、`KeepAlive` があるのに launchd が起こさなかったことが実際にある。止まっている間
スマホからは預けられないが、Mac 側では誰も気づけない。

見る・直すの中身は relay 側の `relay doctor --json` / `--fix` が持ち、Mulmo Control
（`scripts/mulmo-relay`）は呼ぶ回数と、何をしたかの控えだけを持つ。**押すのは人、戻すのは
機械で、起こしても直らないものは回さない。** トンネルの生死は `tunnel-agent`（launchctl の
状態）で見る。外からの到達の 401 は、Access が前にいるとトンネルが止まっていても返る
（relay 側で実測）ので、緑の根拠にしない。

| # | gate | 何を守るか | どう見るか |
|---|---|---|---|
| 178 | fast | `relay` が無い人には行を出さず、シェルも起こさない | ✅ 本物の `mulmo-relay` を偽の HOME で relay の無い状態で走らせ、`no-cli` と書くことを見る。画面の行が `relayInstalled` で囲まれていること、ensure が relay の有無を見てから起こすこと（#38）、画面が探す場所とスクリプトの PATH が揃っていることも見る（#214） |
| 179 | fast | 落ちていたら自動で `relay doctor --fix` を呼び、何をいつ起こしたかを状態に残す | ✅ 偽の relay（トンネルが落ちた世界）で ensure し、`--fix` が1回呼ばれ、状態が ok に戻り、`event=fixed`・理由の項目・時刻が残ることを見る。通っているときは呼ばないことも見る（#214） |
| 180 | fast | 短時間に何度も起こす羽目になるなら自動をやめ、「直す」で再開できる | ✅ 起こしても次に見るとまた落ちる偽の relay で、上限（`FIX_LIMIT`、定数）まで起こし、その後は呼ばずに `halted` になること、「直す」で `--fix` を呼び、自動も再開することを見る（#214） |
| 181 | fast | 起こしても直らない失敗（登録漏れ・LaunchAgent が無い）では `--fix` を呼ばない | ✅ 偽の relay で登録漏れだけ・LaunchAgent 無しの世界を作り、`--fix` が呼ばれず `warn` になることを見る（#214） |
| 182 | fast | リレーの知らせとボタンが、状態ごとに対応表どおり出る | ✅ `relayOK` / `relayButtonTitle` / `relayNotes` を `StatusDisplay.swift` ごと組んで13通り**実際に走らせる**（1日より古い「起こしました」を引っ込める・直せない失敗にボタンを出さない・止めたまま通っているときの再開の口を含む）。画面がその3本を通っていることも見る（#214） |
| 183 | fast | 起動時・巡回・パネルを開いたときにリレーを見て、書き置きの場所が揃っている | ✅ `init()` と `refresh()` から `ensureRelayIfDue` を呼んでいること、アプリの中の写し（`tool()`）から呼んでいること、`relay.json` の場所が画面とスクリプトで同じことを見る（#214） |
| 184 | fast | `relay doctor --json` を知らない古い relay でも、行が壊れない | ✅ 文字で答える偽の relay で ensure と fix を走らせ、`old`（更新が要ります）に落ちることを見る。押しても同じ答えしか返らないボタンは出さない（#214） |
| 185 | fast | 常駐は動いているのに接続だけ0本（`tunnel-ready` が down）でも起こす | ✅ 偽の relay で接続0本の世界を作って ensure し、`--fix` が呼ばれ、状態が ok に戻り、理由に `tunnel-ready` が残ることを見る。他の項目（常駐・外からの到達）は通っているので、直せる項目に無いと一番捕まえたい形で起こさない（#216） |
| 186 | fast | 接続の本数が未確認なだけでは起こさない | ✅ 偽の relay で `tunnel-ready` を未確認（ok 扱い）にして ensure し、`--fix` が呼ばれず状態が ok のままであることを見る。未確認は relay 側が別の cloudflared を拾わないよう決めなかっただけで、kickstart で接続を切り直す理由にならない（#216） |

### 消した項目（117〜119・122〜125・127）

#152 で作った「プロジェクト単位の登録」を丸ごと外したため、`projects.json` を
守る項目（117・122・124・125）、行の対応表（118）、はずす前に止める順序（123）、
登録が無いときの説明（127）、スマホから見えること（119）は対象そのものが無く
なった。**番号は再利用しない。** 次に足すなら 190 から。

外した理由は「危険だったから」ではない。**求められていない機能だった**（#160）。
守る対象が消えたので、守る項目も消す。

## 現状

| 状況 | 件数 |
|---|---|
| ✅ `check.sh` が見ている | 138 |
| 🔨 実装はあるが自動検査なし | 6 |
| ⛔ 無効 | 1 |
| 未着手 | 36 |

101 は 100番（新しい壊れ方を見つけたらここへ戻す）を1周させて足したもの。
自己更新の写しが `mulmo-config-get` しか連れて行かないことに気づいたが、
そのときは自己更新そのものを触るのが危ないと判断して手を出さなかった。
**壊れ方だけ先に検査へ落としてある。**

**実装があることと、壊れたら気づけることは別。**「作者の環境では正しく見える」
不具合が1日で6件出たのは後者が無かったためで、🔨 を ✅ に移すのがこの表の使い道。

残る 🔨 6件は、`check.sh` では見られないもの。

- **060** は自動起動した MulmoTerminal の**見え方**そのもの。ロケールを補う行が
  あることは見ているが、日本語が潰れないことは画面を見ないと分からない
- **098 / 100** は人の運用（Issue に方針を書く・見つけた壊れ方をここへ戻す）で、
  コードの形では表せない
- **162** は GitHub 側から本当に出せるか。Secrets は check.sh から見えない。v1.0.69 で1回通したが、証明書や App用パスワードを更新したときは、また押して確かめるしかない
- **157** は画面の高さそのもの。器を1枚にまとめたことは 156 が見ているが、**本当に
  収まるかは描いてみないと分からない。** 作者の画面で収まっても小さいノートでは
  はみ出すので、#6 と同じ穴でもある

無理に ✅ にしない。**印が実態と違うことのほうが害が大きい**（085 で一度踏んだ）。

## 新しい壊れ方を見つけたら

項目を足して `check.sh` に検査を入れる（100番）。**検査を書いたら必ず
「壊したら落ちるか」を試すこと。** 同日中に4回、効いていない検査を
「効いた」と誤読しかけた。踏んだ罠:

- 変数名で探すと、その話をしている**自分のコメント**に当たる
- `grep -v` で行を消して否定テストを作ると**構文エラー**になり、検査の手前で落ちる
- コメント行をシェバンより前に置くと、シェバン検査ごと飛ばされる
- `grep -vE ':[[:space:]]*(#|//)'` は **URL の `://`** に当たる
- **カタログの印そのものを間違えることがある。** 085 は `check.sh` が既に見ていたのに
  🔨 と書いていた。印を付ける前に、対応するガードが実在するか `check.sh` を検索する
- 消す対象の綴りを確かめずに `sed` すると、何も消えないまま「素通り」と出る
- **zsh は引用符を外しても単語に分かれない**（sh と違う）。引用符を外して壊れないことは、
  検査が効いていない証拠にならない。空白で実際に壊れるのは #32 の形 ——
  コマンドを1本の文字列に組んで `zsh -c` に渡すとき
