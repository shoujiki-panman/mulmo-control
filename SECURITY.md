# セキュリティと AI QA ループ

Mulmo Control はローカルツールの起動、停止、インストール、更新を扱います。スクリプト、更新経路、LaunchAgent、ログ、インストール先に触る変更は、再現できる検査を `check.sh` に入れてから直します。

## ローカルゲート

```bash
./check.sh
```

このゲートでは、スクリプト構文、同梱スクリプト数、空白入りアプリパス、古い保存先への逆戻り、パッケージ版の一致、アプリ署名、`app-info.env` / network-to-shell pipe / `eval` / `sudo` まわりのセキュリティ回帰を見ます。

AI エージェントの入口は次です。

```bash
./qa/diagnose
./qa/check
./qa/next
```

## AI に直させる流れ

AI エージェントにバグ修正を頼むときは、次の順番にします。

1. 先に失敗する検査を追加、または既存検査を拡張する。
2. その検査を通す最小の修正を入れる。
3. `./check.sh` を実行する。
4. 同じ種類のバグが再発しうるなら、検査を残す。

`app-info.env` は shell ではなくデータとして扱います。値は `scripts/mulmo-control-config` 経由で読み、`source` や `.` で直接実行しません。
