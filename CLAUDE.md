# CLAUDE.md — mulmo-control

最初に `START-HERE.md` と `STATUS.md` を読む。終える前に `STATUS.md` を今日の状態に更新する。
Issue → ブランチ → 実装 → チェック → PR → マージ の1周を崩さない。1 Issue = 1 PR。

## 検査

- `./check.sh` … 配る前・PR の前に通す全部（Swift のビルドと codesign が要るので macOS）
- `./check-deploy.sh` … 下の「置き場所と公開範囲」だけ。zsh だけで動く。`.claude/settings.json` の hook が、ファイルを書くたびに回す
- 検査を足したら**壊して落ちることを確かめる**。カタログは `SECURITY.md`。新しい壊れ方を見つけたら 100番の手順で戻す

## 置き場所と公開範囲

元にした記事: https://zenn.dev/singularity/articles/vibe-coding-deploy-and-security

公開範囲: A
段: 1

- 公開範囲は **A**（利用者それぞれが、自分の Mac で自分のために動かす）
- 段は **第1段**（手元の PC。公開するものはゼロ）
- データ（預かった鍵・ログ・設定）は**その Mac から出ない**。ネットに向かうのは GitHub Releases と npm を「取りに行く」向きと、MulmoTerminal / MulmoClaude の remote host に「繋ぎ直す」向きだけ。こちらから何かを待ち受けて外に見せることはない
- この2行は `check-deploy.sh` が読む。**上げるのは人の判断**で、上げる PR は宣言の変更と `SECURITY.md` O 節の守りをいっしょに持つ

### AI セッションが守ること

1. **段を飛ばさない。** 手元の PC → 社内ネットワーク → 入口と出口を既存ツールに任せる → それでも足りなければクラウド。頼まれていないのに Supabase / Firebase / Vercel / Cloudflare / Docker の足場を足さない。「〇〇できるようにして」に対する最短のコードは、たいてい外に置く形になる。最短を選ぶ前に、下の段で足りないかを一度問う
2. **待ち受けは 127.0.0.1 だけ。** `0.0.0.0` で待たない。第2段（同じネットワークから見える）は、ノートを持ち出したとき隣の人からも見える
3. **鍵をコードに書かない。** ブラウザ用でない鍵（API キー・`service_role`・Admin SDK・Bot トークン・Webhook の URL）は Keychain か `.env` に置く。`.env` は追跡しない。読んだ鍵を `echo` / `tee` / ログに出さない（109番）
4. **ブラウザに配る名前に秘密を入れない。** `VITE_` / `NEXT_PUBLIC_` で始まる名前は「公開する」という意味。環境変数に入れても公開は公開
5. **ファイルの置き場所を公開にしない。** 渡すなら期限つきの URL。受け取るなら、ログインの後ろに名簿を置く。「URL を知っている人だけ」は認証ではない
6. **境界線は人が引く。** 「この中身が全部そのまま流出したら困るか」を問うのは作った本人で、AI は決めない。困るものはブラウザから直接触れる場所に置かず、判断は自分のサーバー側でする。設定画面の表示（「制限: 有効」）を信じず、実際に叩いて確かめる

### 記事の3行

> 画面に鍵をかけても、データのドアは別に開いている。
> 出たら困るものは、そもそも外に置かない。
> クラウドは、下の段で足りないとわかってから。
