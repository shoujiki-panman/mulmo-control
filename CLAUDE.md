# Claude Code Instructions

Read `AGENTS.md` first and follow it. This file exists because Claude Code looks for `CLAUDE.md`; the canonical agent workflow is in `AGENTS.md`.

Before changing code, run:

```bash
./qa/diagnose
./qa/check
./qa/next
```

Issue #67 is the work hub for AI QA and security:

https://github.com/shoujiki-panman/mulmo-control/issues/67
